version: '3.7'

services: 
  nginx:
    build: ./nginx
    volumes:
      - static:/static
    ports:
      - "80:8000"
    depends_on:
      - recipe

  recipe:
    volumes:
      - static:/static
    build:
      context: .
    ports:
      - "8000:8000"

volumes:
    static: